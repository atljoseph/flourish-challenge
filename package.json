{
  "name": "flourish-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lib": "cd lib && npm run build",
    "etl": "npm run db && npm run lib && cd etl && rm -rf node_modules && npm i && npm run start",
    "api": "npm run lib && cd api && rm -rf node_modules && npm i && npm run start",
    "db": "cd db && npm run stop && npm run start",
    "dbList": "cd db && npm run dbList",
    "dbStrains": "cd db && npm run dbStrains"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "util": "^0.12.1"
  }
}
